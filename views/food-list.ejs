<script>
    function goBack() {
      window.history.back();
    }
</script>
  
<a onclick="goBack()" class="previous">← Back</a>

<h2>Food list</h2>

<ul>
<% foods.forEach(function(food){ %>
    <li>
        <a href="/food/<%= food._id %>"><%= food.name %> - <%= food.quantity %> <%= food.quantity_type %></a>
    </li>
<% }); %>
</ul>